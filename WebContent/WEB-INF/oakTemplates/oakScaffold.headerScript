webAppContextPath = '__webAppContextPath__';
var htmlElement = document.getElementsByTagName('html')[0];
if(htmlElement.removeAttribute) { 
	htmlElement.removeAttribute('class');
}
if(htmlElement.classList) {
	htmlElement.classList.add('js');
}
else if(htmlElement.setAttribute) {
	htmlElement.setAttribute('class', 'js');
	htmlElement.setAttribute('className', 'js');
}